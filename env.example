# Variables de entorno para PostgreSQL
# Copia este archivo a .env y configura los valores para produccion

# ============================================
# CREDENCIALES DE POSTGRESQL (OBLIGATORIO)
# ============================================
# IMPORTANTE: Cambiar estos valores en produccion
POSTGRES_USER=training_user
POSTGRES_PASSWORD=tu_password_seguro_aqui
POSTGRES_DB=training_db

# ============================================
# NOTAS DE CONFIGURACION
# ============================================
# La URL de conexion para el backend sera:
# DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
#
# En EasyPanel, el hostname 'postgres' sera el nombre del servicio.
# Si usas otro nombre, ajusta la URL acordemente.











